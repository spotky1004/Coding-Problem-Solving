/**
 * @param {(number | bigint)[]} a 
 * @param {(number | bigint)[]} b 
*/
function convolution(a, b) {
  
}

/**
 * @param {(number | bigint)[]} a 
*/
function fft(a) {
  const N = 2**Math.ceil(Math.log2(a.length + b.length));
}
